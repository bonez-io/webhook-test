# webhook-test

Throwaway repo for exercising the bonez-pipeline GitHub push webhook
(`feat/github-webhook-trigger`). Push commits here and watch the incremental /
full reindex fire. See the runbook in `bonez-pipeline/docs/RUNBOOK-webhook-test.md`.
